import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Fase 1: configuración mínima.
  // La integración de Supabase y ajustes de imágenes se añadirán en fases posteriores.
};

export default nextConfig;
